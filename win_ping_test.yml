---
- name: win_ping_test
  hosts: all
  tasks:
    - name: Debug Messages
      debug:
        msg: 
          - "Ansible Facts"
          - "ansible_fact = {{ ansible_user }}"

    - name: Print out all morpheus vars
      debug: 
        msg: "{{ hostvars[inventory_hostname]['morpheus']['server'] | to_nice_yaml }}"

  roles:
    - win_ping_test
      